events {
  worker_connections  1024;
}

http {
  error_log  /dev/stderr info;

  server {
    listen 80;

    location /api {
      rewrite ^/api/(.*)$ /$1 break;
      proxy_pass http://{{ip}}:8000;
    }

    location / {
      proxy_pass http://{{ip}}:3000;
    }
  }
}